package com.synapse.social.studioasinc.core.media.models

/**
 * Defines the context/destination for a media upload.
 */
enum class MediaUploadType {
    AVATAR,
    COVER_PHOTO,
    POST,
    STORY,
    REEL,
    CHAT_ATTACHMENT,
    OTHER
}
